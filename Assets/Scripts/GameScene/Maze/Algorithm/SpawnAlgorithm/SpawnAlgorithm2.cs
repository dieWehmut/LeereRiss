using UnityEngine;

public class SpawnAlgorithm2 : ISpawnAlgorithm
{
	public void Spawn(GameObject entity, MazeGenerator generator)
	{
		if (entity == null || generator == null)
		{
			return;
		}

        //
	}
}
